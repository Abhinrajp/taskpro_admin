import 'package:flutter/material.dart';

const primarycolour = Color.fromARGB(255, 4, 8, 45);

// Homescreen gradients

const List<Color> maingradient = [
  primarycolour,
  Colors.white,
];
const Alignment mainBegin = Alignment.topCenter;
const Alignment mainEnd = Alignment.bottomCenter;

// Homescreen listview gradient

List<Color> listviewgradien = const [
  Color.fromARGB(255, 126, 130, 162),
  Color.fromARGB(239, 228, 229, 230),
];
const Alignment listBegin = Alignment.topRight;
const Alignment listEnd = Alignment.bottomLeft;
